Non-backwards compatible changes since previous version:

SXMatrix:
Constructor SXMatrix("name",n,m) -> symbolic("name",n,m)
input/output -> gone (inputStruct, internal class)
getOutputData etc (in python getOutput) -> output 
inf -> infSX
ones -> onesSX
zeros -> zerosSX
eye -> eyeSX
horzcat(x,y)/vertcat(x,y) no longer available from python (use horzcat((x,y))/vertcat((x,y)) instead)
integrator has additional inputs and outputs (algebraic state)
getArray in FunctionIO -> toArray in Matrix<>
getMatrix in FunctionIO -> toMatrix in Matrix<>
